
#  if [ "${schema_setup}" == "mysql" ]; then
#  func_print_head "Install MySQL Client"
#  yum install mysql -y &>>$log_file
#  func_stat_check $?
#
#  func_print_head "Load Schema"
#  mysql -h mysql-dev.msdevops72.online -uroot -p${mysql_root_password} < /app/schema/shipping.sql &>>$log_file
#  func_stat_check $?
#  fi

- name : Load MongoDB Schema
  when: schema_setup == "mongodb"
  block:
    - name: Copy MongoDB repo
      ansible.builtin.copy:
        src: mongo.repo
        dest: /etc/yum.repos.d/mongo.repo

    - name: Install MongoDB Client
      ansible.builtin.yum:
        name: mongodb-org-shell
        state: installed

    - name: Load Schema
      ansible.builtin.shell: mongo --host mongodb-dev.msdevops72.online </app/schema/{{component}}.js